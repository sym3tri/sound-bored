// For NodeJS/RequireJS compatibility.
//if (typeof define !== 'function') { var define = require('amdefine')(module); }

//var requirejs = require('requirejs');
//var expect = require('chai').expect;
//requirejs.config({
  //baseUrl: './public/js/',
  //paths: {
    //'templates':            '../templates',
    //'text':                 '../extern/require/text',
    ////'jquery':               '../extern/jquery/jquery-1.7.2.min',
    //'backbone':             '../extern/backbone/backbone',
    //'underscoreBase':       '../extern/underscore/underscore',
    //'underscore':           'lib/underscore',
    //'handlebars':           'lib/handlebars'
  //},
  //// Load modules that don't natively support AMD.
  //shim: {
    //'underscoreBase': {
      //deps: [],
      //exports: '_'
    //},
    //'backbone': {
      //deps: ['underscore', 'jquery'],
      //exports: 'Backbone'
    //}
  //}
//});

//requirejs(['app'], function (App) {
  //'use strict';

  //describe('app.js', function () {

    //beforeEach(function () {
    //});

    //afterEach(function () {
    //});

    //it('loads', function () {
      //expect(true).not.to.equal(undefined);
    //});

    //it('passes', function () {
      //expect(true).to.equal(true);
    //});

  //});

//});
